<UserControl x:Class="MyTeams.UI.Controls.NavigationButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyTeams.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
  <DockPanel>
    <TextBlock DockPanel.Dock="Left" Width="5" Background="Aquamarine"/>
    <StackPanel DockPanel.Dock="Left" Orientation="Vertical" HorizontalAlignment="Center">
      <Image Source="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ImageUrl}" Height="32" />
      <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Text}"/>
    </StackPanel>
  </DockPanel>
</UserControl>
