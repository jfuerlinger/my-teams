<Window x:Class="MyTeams.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyTeams.WpfApp" xmlns:ctrls="clr-namespace:MyTeams.WpfApp.Controls"
        xmlns:viewmodels="clr-namespace:MyTeams.WpfApp.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
  <Window.Resources>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="70"/>
      <ColumnDefinition Width="300"/>
      <ColumnDefinition Width="5"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <ctrls:MainNavigationControl Grid.Row="0" Grid.Column="0" />
    <ctrls:TeamsOverviewControl Grid.Row="0" Grid.Column="1"  DataContext="{Binding TeamsOverviewViewModel}"/>
    <GridSplitter Grid.Row="0" Grid.Column="2" HorizontalAlignment="Stretch" />

  </Grid>

</Window>
